name: PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  pull-requests: read
  contents: read

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: badMade/agy-pr-review@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
